<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".user.locations.AddUserLocationActivity"
    tools:showIn="@layout/activity_new_user_location">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="@dimen/activity_vertical_margin"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:context=".user.locations.AddUserLocationActivity"
        tools:showIn="@layout/activity_new_user_location">

        <include layout="@layout/content_add_location" />

        <Button
            android:id="@+id/button_save_location"
            android:layout_width="130dp"
            android:layout_height="45dp"
            style="@style/FirebaseUI.Button"
            android:layout_below="@id/google_attribution_logo"
            android:layout_margin="20dp"
            android:textColor="@color/button_text"
            android:text="@string/button_save_location"
            android:enabled="false"
            android:onClick="saveLocation"
            android:layout_centerHorizontal="true"/>
    </RelativeLayout>

</ScrollView>